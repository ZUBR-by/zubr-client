/**
 * A conversion list for BE query
 * @description
 * @export
 * @enum {number}
 */
export enum columnToQuery {
  firstName = 'FIRST_NAME',
  lastName = 'LAST_NAME',
  address = 'ADDRESS',
  insuranceNumber = 'INSURANCE_NUMBER',
}
