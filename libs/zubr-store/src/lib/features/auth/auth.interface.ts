export interface Auth {
  /**
   * Auth access token
   * @description
   * @type {string}
   * @memberof Auth
   */
  accessToken: string;
  /**
   * Auth refresh token
   * @description
   * @type {string}
   * @memberof Auth
   */
  refreshToken: string;
}
